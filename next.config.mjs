/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: false,
    images: {
        domains: ['ulrirxobkbomwcackkmt.supabase.co'], // Añade el dominio aquí
      },
};

export default nextConfig;
